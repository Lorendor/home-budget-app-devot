<?xml version="1.0"?>
<ruleset name="Home Budget API">
    <description>PHPCS configuration for Home Budget API</description>
    
    <!-- Files to check -->
    <file>app</file>
    <file>tests</file>
    <file>routes</file>
    <file>database/migrations</file>
    <file>database/seeders</file>
    
    <!-- Exclude vendor and other directories -->
    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/node_modules/*</exclude-pattern>
    <exclude-pattern>*/storage/*</exclude-pattern>
    <exclude-pattern>*/bootstrap/cache/*</exclude-pattern>
    
    <!-- Use PSR-12 standard -->
    <rule ref="PSR12"/>
    
    <!-- Allow snake_case method names in tests (Laravel standard) -->
    <rule ref="PSR1.Methods.CamelCapsMethodName">
        <exclude-pattern>*/tests/*</exclude-pattern>
    </rule>
    
    <!-- Additional rules -->
    <rule ref="Generic.Files.LineLength">
        <properties>
            <property name="lineLimit" value="120"/>
            <property name="absoluteLineLimit" value="140"/>
        </properties>
    </rule>
    
    <!-- Show progress -->
    <arg name="colors"/>
    <arg name="parallel" value="8"/>
</ruleset>
